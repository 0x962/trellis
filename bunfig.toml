# Every Expo tool resolves one copy of each package from node_modules. The
# isolated linker makes one copy of Expo for each peer set.
[install]
linker = "hoisted"

# `apps/server/scripts/testPreload.ts` states why a test process runs it first.
[test]
preload = ["./apps/server/scripts/testPreload.ts"]
