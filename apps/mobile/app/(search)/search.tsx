import { EmptyState } from "../../src/components/EmptyState";

export default function SearchScreen() {
	return <EmptyState title="No recent searches" hint="Search by identifier, title, or text." />;
}
