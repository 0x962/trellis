// The time on a row: "now", "9m", "2h", "1d", then a short date.
export const compactRelativeTime = (_iso: string, _now: Date = new Date()): string => {
	throw new Error("mobile-inbox: compactRelativeTime is not implemented");
};

// Digits grouped in the device locale: 1,234.
export const formatCount = (_count: number): string => {
	throw new Error("mobile-inbox: formatCount is not implemented");
};
