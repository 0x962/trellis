export type AttachmentBoxProps = {
	// The ticket a picked file attaches to: CDE-42.
	ticket: string;
};

// The explicit dashed drop box with its file picker.
export function AttachmentBox(_props: AttachmentBoxProps) {
	return null;
}
