// Open tickets whose open pull request holds a failing check. The row names
// the pull request and the failing checks, and copies a re-run command.
export function FailingCiSection() {
	return null;
}
