// Tickets in a human-reviewer status, oldest waiting first. `a` approves a
// row into the lowest-position done status; `r` opens the send-back box.
export function ReviewSection() {
	return null;
}
