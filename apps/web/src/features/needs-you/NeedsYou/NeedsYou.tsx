// The home screen: everything that waits on a person, in four sections.
// The page reads `inbox.get` once with no project filter and hands each
// section its own rows.
export function NeedsYou() {
	return null;
}
